# Custom site-wide data, in lieu of modifying (and risking conflict) with `_config.yml`

# Custom `author` property

author:
  name: &name Karl Stolley
  email: kstolley@iit.edu

social:
  github: profstolley
  twitter: stolleydotdev

# Replacement for `site.title`, from `_config.yml`
# Reference `site.data.custom.title` instead
title: *name

# Uncomment to use with the navigation include in `_includes/`
# Pages will be listed in the order shown here:

navigation:
  - resume.md
  - projects.md
  - index.md
